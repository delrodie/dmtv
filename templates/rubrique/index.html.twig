{% extends 'backoffice_layout.html.twig' %}

{% block title %}{{ parent() }} Gestion des rubriques{% endblock %}

{% block body %}
    <div class="page-content-wrapper dashboard">

        <div class="container-fluid">

            <div class="row">
                <div class="col-sm-12">
                    <div class="page-title-box">
                        <h4 class="page-title">Gestion des rubriques </h4>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-md-12 col-xl-9">
                    <div class="card">
                        <div class="card-body">

                            <h4 class="mt-0 header-title">Liste des rubriques</h4>

                            <table class="table table-striped table-bordered" cellspacing="0" width="100%">
                                <thead>
                                <tr>
                                    <th class="text-center">ID</th>
                                    <th class="text-center">RUBRIQUES</th>
                                    <th class="text-center">ACTIONS</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for rubrique in rubriques %}
                                    <tr>
                                        <td class="text-right mr-3">{{ loop.index }}</td>
                                        <td>{{ rubrique.libelle }}</td>
                                        <td class="text-center">
                                            <a href="{{ path('rubrique_edit', {'id': rubrique.id}) }}"><i class="fa fa-edit"></i></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>

                        </div>
                    </div>
                </div>
                <div class="col-md-12 col-xl-3">
                    <div class="card m-b-30">
                        <div class="card-body">

                            <h4 class="mt-0 header-title">Enregistrerment</h4>

                            {{ include('rubrique/_form.html.twig') }}

                        </div>
                    </div>
                </div>

            </div>

        </div>

    </div>

{% endblock %}
